# Telegram Quiz Bot

Телеграм бот с викториной, развернутый на Vercel с мини-приложением для игры.

## Особенности

### Телеграм Бот
- 🎮 Кнопка "Играть" - запуск мини-приложения
- 📊 Моя статистика - персональная статистика игрока
- 🏆 Списки лидеров - топ игроков
- ℹ️ Информация/Поддержка - справка о игре

### Игра (Мини-приложение)
- 🎯 10 случайных вопросов из большой базы (100+ вопросов)
- ⏱️ 15 секунд на каждый вопрос
- 💡 2 подсказки "50/50" за игру
- 📱 Адаптивный дизайн для мобильных устройств
- 🎨 Красивые анимации и переходы
- 📊 Детальная статистика результатов

### Категории вопросов
- История России (20 вопросов)
- География (20 вопросов)
- Наука (20 вопросов)
- Спорт (20 вопросов)
- Литература (20 вопросов)

## Настройка Supabase

### 1. Создание проекта в Supabase
1. Перейдите на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Дождитесь инициализации базы данных

### 2. Создание таблиц
1. Откройте SQL Editor в Supabase Dashboard
2. Скопируйте и выполните содержимое файла `supabase-schema.sql`
3. Убедитесь, что таблицы `users` и `games` созданы

### 3. Получение ключей
1. Перейдите в Settings → API
2. Скопируйте:
   - `Project URL` (SUPABAхоSE_URL)
   - `anon public` ключ (SUPABASE_ANON_KEY)

### 4. Настройка переменных окружения
Добавьте в Vercel Environment Variables:
- `SUPABASE_URL` - URL вашего проекта
- `SUPABASE_ANON_KEY` - публичный ключ

### 1. Подготовка
1. Создайте бота в Telegram через @BotFather
2. Получите токен бота
3. Форкните или скачайте этот репозиторий

### 2. Настройка переменных окружения
В Vercel добавьте переменные окружения:
- `BOT_TOKEN` - токен вашего Telegram бота
- `WEBAPP_URL` - URL вашего приложения на Vercel (например: https://your-app.vercel.app)

### 3. Развертывание
1. Подключите репозиторий к Vercel
2. Vercel автоматически развернет приложение
3. Получите URL приложения

### 4. Настройка вебхука
После развертывания настройте вебхук для бота:
```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
     -H "Content-Type: application/json" \
     -d '{"url": "https://your-app.vercel.app/api/bot"}'
```

### 5. Настройка мини-приложения
В @BotFather:
1. Выберите вашего бота
2. Выберите "Bot Settings" → "Menu Button"
3. Установите URL: `https://your-app.vercel.app/game`

## Структура проекта

```
├── api/
│   ├── bot.js          # Основной файл бота
│   └── database.js     # Работа с базой данных
├── public/
│   └── game/
│       ├── index.html  # HTML игры
│       ├── styles.css  # Стили игры
│       ├── game.js     # Логика игры
│       └── quiz-data.js # База вопросов
├── package.json
├── vercel.json
└── README.md
```

## База данных

Используется SQLite для хранения:
- Пользователи и их статистика
- Результаты игр
- Рейтинги

## Локальная разработка

```bash
# Установка зависимостей
npm install

# Создание .env файла
echo "BOT_TOKEN=your_bot_token_here" > .env
echo "WEBAPP_URL=http://localhost:3000" >> .env

# Запуск (только для тестирования API)
npm start
```

**Важно:** Бот работает только на продакшене с настроенным вебхуком!

## Добавление новых вопросов

Отредактируйте файл `public/game/quiz-data.js`:

```javascript
"Новая категория": [
    {
        question: "Текст вопроса?",
        answers: ["Вариант 1", "Вариант 2", "Вариант 3", "Вариант 4"],
        correct: 0 // Индекс правильного ответа (0-3)
    }
]
```

## Технологии

- **Backend:** Node.js, node-telegram-bot-api, SQLite
- **Frontend:** Vanilla JavaScript (ES6+), HTML5, CSS3
- **Hosting:** Vercel
- **Database:** SQLite (в продакшене в /tmp/)

## Поддержка

Для вопросов и предложений создавайте Issues в репозитории.